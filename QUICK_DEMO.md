# 🚀 Быстрый старт - Одна команда

## Для демонстрации жюри

**Все в одной команде:**

```bash
python3 prepare_data.py --demo --demo-clean 20 --demo-fraud 20 --streamlit
```

**Что произойдет:**
1. ✅ Загрузит данные из `data/` (транзакции + поведенческие паттерны)
2. ✅ Применит feature engineering (50 признаков)
3. ✅ Создаст демо-выборку (20 легитимных + 20 мошеннических)
4. ✅ Автоматически запустит Streamlit

**Затем в Streamlit:**
1. Выберите режим "Пакетная проверка"
2. Загрузите `demo_batch_ready.csv`
3. Нажмите "Проверить все транзакции"
4. Покажите результаты жюри! 🎯

---

## Альтернативные варианты

### Только подготовка данных (без Streamlit)
```bash
python3 prepare_data.py --demo
```

### Запуск Streamlit отдельно
```bash
streamlit run app.py
```

### Создать большую выборку
```bash
python3 prepare_data.py --demo --demo-clean 50 --demo-fraud 50 --streamlit
```

---

**Готово! 🏆**
